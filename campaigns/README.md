# Campaigns

각 캠페인은 독립 폴더로 관리합니다.

## 구조
- `TRPG_Campaign_State.xlsx` : 핵심 상태 (Players, NPCs, Quests, Timeline, World_Facts, Session_Log)
- `timeline.json` : 빠른 조회용
- `logs/` : 세션 로그 (markdown)

## 새 캠페인 생성
1. `default` 폴더를 복사
2. 이름을 변경
3. xlsx 초기화
