name: Validate Campaign
on:
  push:
    paths:
      - 'campaigns/**'
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate structure
        run: |
          echo "TODO: Add xlsx schema validation"
